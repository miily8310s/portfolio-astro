---
type Props = {
  date: Date
}

const { date } = Astro.props
---

<time class="dark:text-white" datetime={date.toISOString()}>
  {date.toLocaleDateString("en-US", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })}
</time>